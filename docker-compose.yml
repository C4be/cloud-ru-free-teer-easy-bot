version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - notes_data:/app/data
    environment:
      DB_PATH: /app/data/notes.db

volumes:
  notes_data:
